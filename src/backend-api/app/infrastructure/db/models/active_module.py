# STUB — active_modules table was dropped in migration 0015.
# Story 12.3 will remove all references to ActiveModule.


class ActiveModule:
    """Dropped in migration 0015. Vehicle module is the only module and needs no registry."""
    pass
